{
    "connections": {
        "default": "sqlite://database.db"
    },
    "apps": {
        "models": {
            "models": ["models.users", "models.user_courses", "models.invoices"],
            "default_connection": "default"
        }
    }
}
